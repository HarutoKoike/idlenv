#!/bin/zsh

PACKAGES=($(find $IDL_PACKAGE_PATH -depth 1 -type d))

for pkg in "${PACKAGES[@]}"; do
  json=$pkg/idlpackage.json
  if [ -f $json ]; then
    grep -o '"Name": "[^"]*' $json | grep -o '[^"]*$'
  fi
done
